---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: testnet
  children:
    mina:
      hosts:
        ts.validator.mina.testnet.encapsulate.xyz:
          type: validator
